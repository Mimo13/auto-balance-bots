/**
 * Convenience re-export for the markdown report formatter.
 * CLI can `import { formatAdvisorMarkdown } from './markdown-report.js'`.
 */
export { formatAdvisorMarkdown } from './advisor-report.js';
